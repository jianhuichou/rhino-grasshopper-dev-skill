C# GHA scaffold template marker.

Use scripts/scaffold_csharp_gha.py to generate the concrete project files with
fresh GUIDs. This asset folder exists so future iterations can add richer
boilerplate without expanding SKILL.md.
